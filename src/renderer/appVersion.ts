/**
 * Application semver from root `package.json`, inlined at dev/build time by Vite
 * (`define.__FLOWSWITCH_APP_VERSION__`). Matches Electron-builder / release tag naming.
 */
export const APP_VERSION = __FLOWSWITCH_APP_VERSION__;
