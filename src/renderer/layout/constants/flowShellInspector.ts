/**
 * Width reserved for the fixed right inspector drawer.
 * Keep drawer `w-*` and main column `mr-*` in sync so content is not covered.
 */
export const FLOW_SHELL_INSPECTOR_WIDTH_CLASS = "w-[clamp(18rem,24vw,24rem)]";

export const FLOW_SHELL_INSPECTOR_MARGIN_CLASS = "mr-[clamp(18rem,24vw,24rem)]";
